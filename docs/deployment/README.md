# Deployment docs (server)

This folder holds the server-side deployment notes. Each topic is one page.

## Pages

- [reverse-proxy.md](reverse-proxy.md) — how to run the server behind a reverse proxy, and how
  to serve one build from any URL prefix. The proxy sends the prefix through a header.

New deployment topics go here as new pages.
